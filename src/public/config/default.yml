app:
  name: Authentication Internal Service
  path: /authen-service
  security:
    http:
      publicUrls:
        - /actuator/health
        - /actuator/info
        - /public/v1/user
        - /public/v1/user/send-verification-code
        - /public/v1/user/login